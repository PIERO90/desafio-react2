import React from "react";

const Formulario = ({ formData, errors, handleChange, handleSubmit }) => {
  return (
    <form className="form" onSubmit={handleSubmit}>
      {/* ... (your existing code) */}
    </form>
  );
};

export default Formulario;
